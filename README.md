# Plant-Bio-Signal-Communication-Classification
Developed a multi-class ML pipeline combining K-means clustering with supervised models (XGBoost, RF, SVM, MLP) for plant bio-signal communication state classification.
